import s from './SendButton.module.css';

const SendButton = () => {
    return (
        <div className={s.sendbutton}>
            <button >Send</button>
        </div>
    );
}

export default SendButton;