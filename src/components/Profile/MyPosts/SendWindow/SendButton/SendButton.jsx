import classes from './SendButton.module.css';

const SendButton = () => {
    return (
        <div className={classes.sendbutton}>
            <button >Send</button>
        </div>
    );
}

export default SendButton;